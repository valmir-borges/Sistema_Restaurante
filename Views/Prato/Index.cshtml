@model IEnumerable<Sistema_Restaurante.Models.Prato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomePrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrecoPrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fornecedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomePrato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrecoPrato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.NomeCategia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fornecedor.NomeFornecedor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
