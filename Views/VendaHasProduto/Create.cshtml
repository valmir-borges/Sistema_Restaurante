@model Sistema_Restaurante.Models.VendaHasProduto

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>VendaHasProduto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="VendaId" class="control-label"></label>
                <select asp-for="VendaId" class ="form-control" asp-items="ViewBag.VendaId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PratoId" class="control-label"></label>
                <select asp-for="PratoId" class ="form-control" asp-items="ViewBag.PratoId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
